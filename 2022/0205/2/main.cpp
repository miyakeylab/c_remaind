#include <bits/stdc++.h>
using namespace std;
#define all(v) (v).begin(), (v).end()
using pll = pair<long, long>;
#define mp make_pair
#define ff first
#define ss second
#define pb push_back
#define ll long long

signed main()
{
	int n;
	cin >> n;
	int a[n + 1];
	int rad[n + 1];
	for (int i = 0; i < n; i++)
	{
		cin >> a[i];
	}

int max = 0;
	for (int i = 0; i < n; i++)
	{
			cout << a[i];

	}

	return 0;
}
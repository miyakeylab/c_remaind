#include <bits/stdc++.h>
using namespace std;
#define all(v) (v).begin(), (v).end()
using pll = pair<long, long>;
#define mp make_pair
#define ff first
#define ss second
#define pb push_back

signed main()
{
    vector<pair<long, long> > vp;
    string s;
    long k;
    cin >> s;
    cin >> k;
    int dCount = 0;
    for(int i = 0; i < s.size(); i++){
        if(s[i] != "X"){

        }
    }
    return 0;
}